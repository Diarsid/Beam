/*
 * project: Beam
 * author: Diarsid
 */
package com.drs.beam.util.config;

/**
 *
 * @author Diarsid
 */
public enum ConfigParam {
    BEAMCORE_PORT,
    BEAMCORE_HOST,
    CONSOLE_PORT,
    CONSOLE_HOST,
    TASK_MANAGER_NAME,
    EXECUTOR_NAME,
    BEAM_ACCESS_NAME,
    LOCATIONS_HANDLER_NAME,
    WEB_PAGES_HANDLER_NAME,
    CONSOLE_NAME,
    CORE_DB_DRIVER,
    CORE_DB_URL,
    CORE_DB_NAME,
    PROGRAMS_LOCATION,
    IMAGES_LOCATION,
    LIBRARIES_LOCATION
}
